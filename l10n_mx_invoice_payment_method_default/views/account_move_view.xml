<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        DOBLE protección para que "99 Por Definir" siempre aparezca:

        A) A nivel ORM (account_move.py): el campo l10n_mx_edi_payment_method_id
           se redeclara con domain=[] — esto anula cualquier restricción del modelo
           base sin importar la versión de Odoo.

        B) A nivel Vista (este archivo, priority=25): quita el domain de TODOS
           los lugares donde aparezca el campo en la vista de facturas.
    -->

    <!-- Override sobre account.view_move_form (priority alta = se aplica después) -->
    <record id="view_move_form_fix_payment_method_domain" model="ir.ui.view">
        <field name="name">account.move.form.fix.payment.method.domain</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="priority">25</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='l10n_mx_edi_payment_method_id']" position="attributes">
                <attribute name="domain">[]</attribute>
                <attribute name="context">{'active_test': False}</attribute>
            </xpath>
        </field>
    </record>

</odoo>
